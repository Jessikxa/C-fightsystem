#include "test.h"
#include <iostream>
test::test() {
	std::cout << "Constructor called" << std::endl;
	//fightsystem what needed?
	//player class (player info(name, age(if relevant)) and stats (hp, which fighting abilities, does have healing abilities))
	//enemy class(name of enemy and stats(hp, fighting abilities))?
	//fight class (handles the fight between player and enemy)?
	//abilities class (different fighting abilities and healing abilities)? (could be if alles can overlap)
	//inventory class (if needed for healing items or just items in general)?


	





}
